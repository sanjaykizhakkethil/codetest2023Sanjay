

<section class="inner-content sppd pb-5">
  <div class="container">
    <img src="{{url('Uploads')}}/{{$blogs['image']}}" width="200px">
    <h1 class="title-inner text-center mb-2">{{$blogs->name}} </h1>
    <p>{{$blogs->description}}</p>
  </div>
</section>